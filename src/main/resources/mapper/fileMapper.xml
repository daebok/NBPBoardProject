<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyunhye.board.repository.FileRepository">

	<resultMap id="list" type="file">
		<result property="boardId" column="boardId" />
		<result property="fileName" column="fileName" />
		<result property="originName" column="originName" />
		<result property="extension" column="extension" />
	</resultMap>
	
	<insert id="addAttach">
		INSERT INTO FILE (boardId, fileName, originName, extension)
		VALUES
		( (SELECT boardId FROM BOARD ORDER BY boardId desc limit 1) , #{fileName}, #{originName}, #{extension} )
	</insert>
	
	<select id="getAttach" resultMap="list">
		select fileId, fileName, originName from FILE where boardId = #{boardId}; 
	</select>
	
	<delete id="deleteAttach">
		DELETE FROM FILE WHERE fileId = #{fileId}
	</delete>
</mapper>