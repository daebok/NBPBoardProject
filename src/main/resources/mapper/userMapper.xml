<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hyunhye.user.repository.UserRepository">

	<resultMap id="userList" type="user">
		<result property="userNo" column="user_no" />
		<result property="userId" column="user_id" />
		<result property="userPassword" column="user_password" />
		<result property="userName" column="user_name" />
		<result property="userAuthority" column="user_authority" />
	</resultMap>
	
		
	<select id="userSelectList" resultMap="userList">
		SELECT
			user_no,
			user_id, 
			user_name,
			user_authority
		FROM USER
		WHERE user_enabled = 1
		ORDER BY user_no
		LIMIT #{startBoardNo}, #{perPageNum}
	</select>
	
	<select id="userSelectListCount" resultType="int">
		SELECT
			COUNT(user_no)
		FROM USER
		WHERE user_enabled = 1
	</select>
	
	<update id="userAuthorityUpdate">
		UPDATE USER SET
			user_authority = #{userAuthority}
		WHERE 
			user_no = #{userNo}
	</update>
	
	<delete id="userWithBoardDelete">
		DELETE FROM USER
		WHERE 
			user_no = #{userNo}
	</delete>
	
	<update id="onlyUserDelete">
		UPDATE USER SET
			user_enabled = 0
		WHERE
			user_no = #{userNo}
	</update>
	
	<insert id="userInsert">
		INSERT INTO USER (
			user_id,
			user_password,
			user_name
		) VALUES (
			#{userId},
			#{userPassword},
			#{userName}
		)
	</insert>
	
	<insert id="userPasswordChange">
		UPDATE SET USER 
			user_password = #{userPassword}
		WHERE 
			user_id = #{userId}
	</insert>

	<select id="checkIdDuplication" resultType="Integer" parameterType="String">
		SELECT COUNT(*)
		FROM USER
		WHERE 
			user_id = #{userId}
	</select>
	
	<select id="userSelect" resultType="map">
		SELECT
			user_id as username,
 			user_password as password,
			user_enabled as enabled,
			user_authority as authority,
			user_no,
			user_name
		FROM USER
		WHERE
			USER.user_id = #{userId} 
	</select>
	
	<select id="naverUserselect" resultType="int">
		SELECT COUNT(1)
		FROM USER
		WHERE
			USER.user_id = #{userId} 
	</select>
	
	<update id="passwordUpdate">
		UPDATE USER SET
			user_password = #{userPassword}
		WHERE
			user_no = #{userNo}
	</update>
	
	<select id="selectUserInfoSearch" resultMap="userList">
		SELECT
			user_no,
			user_id, 
			user_name,
			user_authority
		FROM USER
		WHERE 
			user_enabled = 1 AND
			user_id = #{userId}	
	</select>
	
</mapper>
