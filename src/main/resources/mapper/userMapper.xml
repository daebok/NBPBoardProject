<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hyunhye.user.repository.UserRepository">

	<resultMap id="userList" type="userDetails">
		<result property="userNo" column="user_no" />
		<result property="userId" column="user_id" />
		<result property="userPassword" column="user_password" />
		<result property="userName" column="user_name" />
	</resultMap>

	<insert id="userRegist">
		INSERT INTO USER (
			user_id,
			user_password,
			user_name
		) VALUES (
			#{userId},
			#{userPassword},
			#{userName}
		)
	</insert>
	
	<insert id="userPasswordChange">
		UPDATE SET USER 
			user_password = #{userPassword}
		WHERE 
			user_id = #{userId}
	</insert>

	<select id="duplicationId" resultType="Integer" parameterType="String">
		SELECT COUNT(*)
		FROM USER
		WHERE 
			user_id = #{userId}
	</select>
	
	<select id="selectUser" resultType="map">
		SELECT
			user_id as username,
 			user_password as password,
			user_enabled as enabled,
			user_authority as authority,
			user_no,
			user_name
		FROM USER
		WHERE
			USER.user_id = #{userId} 
	</select>

</mapper>
