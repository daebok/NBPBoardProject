<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hyunhye.board.boardMapper">

	<resultMap id="list" type="com.hyunhye.board.model.BoardModel">
		<result property="userId" column="userId" javaType="java.lang.Integer" />
		<result property="boardId" column="boardId" javaType="java.lang.Integer" />
		<result property="categoryId" column="categoryId" javaType="java.lang.Integer" />
		<result property="name" column="name" javaType="java.lang.String" />
		<result property="item" column="item" javaType="java.lang.String" />
		<result property="title" column="title" javaType="java.lang.String" />
		<result property="content" column="content" javaType="java.lang.String" />
		<result property="date" column="date" javaType="java.lang.String" />
		<result property="viewCount" column="viewCount" javaType="java.lang.Integer" />
	</resultMap>

	<select id="list" resultMap="list">
		SELECT BOARD.boardId, CATEGORY.item, USER.name, BOARD.title, BOARD.content, BOARD.date, BOARD.viewCount
		FROM BOARD
		<include refid="join_category"></include>
		<include refid="join_user"></include>
		ORDER BY BOARD.viewCount desc
		limit 0, 10
	</select>

	<insert id="regist">
		INSERT INTO BOARD (userId, categoryId, title, content, date)
		VALUES
		(${userId}, (SELECT categoryId FROM CATEGORY WHERE item = #{item}), #{title}, #{content}, #{date})
	</insert>

	<select id="read" resultMap="list">
		SELECT BOARD.boardId, BOARD.userId, USER.name, CATEGORY.item, BOARD.title, BOARD.content, BOARD.date, BOARD.viewCount
		FROM BOARD 
		<include refid="join_category"></include>
		<include refid="join_user"></include>
		WHERE boardId = #{boardId}
	</select>

	<select id="modify" resultMap="list">
		UPDATE BOARD SET
		title = #{title},
		content = #{content},
		date = #{date}
		WHERE boardId = #{boardId}
	</select>

	<delete id="delete"> DELETE FROM BOARD WHERE boardId = #{boardId} </delete>
	
	<select id="listCriteria" resultMap="list">
		SELECT BOARD.boardId, USER.name, CATEGORY.item, BOARD.title, BOARD.content, BOARD.date, BOARD.viewCount
		FROM BOARD
		<include refid="join_category"></include>
		<include refid="join_user"></include>
		<if test="searchType != '' or categoryType != '' ">
			<include refid="search"></include>
		</if>
		ORDER BY BOARD.boardId desc, BOARD.date desc
		limit #{startPage}, #{perPageNum}
	</select>
	
	
	<select id="countPaging" resultType="int">
		SELECT COUNT(boardId) FROM BOARD
		<include refid="join_category"></include>
		<if test="searchType != '' or categoryType != '' ">
			<include refid="search"></include>
		</if>
	</select>
	
	<select id="checkUser" resultType="int">
		SELECT userId FROM BOARD
		WHERE boardId = #{boardId}
	</select>
	
	<update id="increaseViewcount">
		UPDATE BOARD SET viewCount = viewCount + 1 
		WHERE boardId = #{boardId}
    </update>

	<sql id="search">
		<if test="categoryType != 'null'">
			WHERE CATEGORY.item = '${categoryType}'
		</if>
		<if test="searchType != 'null'">
			AND 
			<choose>
				<when test="searchType == 'title_content'">
					( content like '%${keyword}%'
					OR title like '%${keyword}%')
				</when>
				<when test="searchType == 'content'">
					 content like '%${keyword}%'
				</when>
				<when test="searchType == 'title'">
					 title like '%${keyword}%'
				</when>
			</choose>
		</if>
		
	</sql>
	
	<sql id="join_user">
		JOIN  USER
		ON BOARD.userId = USER.userId
	</sql>
	
	<sql id="join_category">
		JOIN CATEGORY
		ON BOARD.categoryId = CATEGORY.categoryId
	</sql>

</mapper>
